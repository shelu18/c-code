#include <stdio.h>

int main()
{
      int a,b,sum;
   printf("enter the numbers a and b\n");
   scanf("%d %d",&a,&b);
    sum= a+b;
    printf("the sum of two numbers is %d+%d=%d",a,b,sum);

    return 0;
}
